repos:
  - repo: local
    hooks:
      - id: dart-format
        name: dart format
        entry: dart format
        language: system
        types_or: [dart]
      - id: flutter-analyze
        name: flutter analyze
        entry: bash -c 'cd app && flutter analyze'
        language: system
        pass_filenames: false
