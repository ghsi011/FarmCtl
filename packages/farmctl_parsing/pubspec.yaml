name: farmctl_parsing
description: Shared parsing utilities for FarmCtl unit tests.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  meta: ^1.11.0

dev_dependencies:
  test: ^1.25.2
  lints: ^4.0.0
