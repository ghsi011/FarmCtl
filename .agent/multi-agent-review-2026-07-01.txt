============================================================
DIM: architecture — FarmCtl's macro-structure is healthy: a clean feature-first layout (view -> providers -> service/repository -> Drift/Dio), an injectable-clock provider, a transactional compare-and-set for alarm decisions, and a well-reasoned foreground-service + WorkManager-watchdog background design with pure, testable helper functions. The prior internal review's architecture fixes (centralized monitor DI via buildMonitorDependencies, value equality on models, nowProvider-based throttling) have genuinely landed. The remaining structural debt concentrates in three places: core/background/thermostat_monitor.d

* [medium|design-improvement|downgraded] core<->features dependency cycle: thermostat_monitor.dart is a 1,025-line god module owning six unrelated concerns @app/lib/core/background/thermostat_monitor.dart:1
  D: core/background/thermostat_monitor.dart imports the feature data layer (thermostat_client/database/repository/service, lines 13-21), while features/thermostats/data/thermostat_service.dart:3-4 imports it back ('show cancelAlarmNotification'), creating a circular import between core/ and features/. The file mixes at least six concerns: foreground-se
  R: Split into three modules with one-way dependencies: (1) core/notifications/ - alarm channels, NotificationAlarmDispatcher, cancelAlarmNotification, showTestAlarmNotification, snooze action-id mapping (no feature imports beyond models); (2) core/background/ - service lifecycle, wa
  V: Facts all confirmed: thermostat_monitor.dart is 1,024 lines and imports five features/thermostats/data files plus settings data/models (thermostat_monitor.dart:13-21), while thermostat_service.dart:3-4 imports it back ('show cancelAlarmNotification')

* [medium|design-improvement|downgraded] Reading-state application logic is triplicated across the three fetch paths (background runner, foreground refresh, save-after-test) @app/lib/features/thermostats/data/thermostat_service.dart:126
  D: The rule 'given a fetch result and previous state, decide out-of-range, persist status, clear snooze/silence on OK, cancel any showing alarm notification, map fetch exceptions to status rows' is implemented three times with slight variations: ThermostatMonitorRunner.run() (thermostat_monitor.dart:597-682), ThermostatService.refresh() (thermostat_se
  R: Extract a single ReadingOutcomeApplier (e.g., in thermostat_reading_utils.dart, which already holds isThermostatReadingOutOfRange and formatOutOfRangeThermostatMessage): applyFetchOutcome({thermostat, previousState, result-or-exception, AlarmPolicy policy}) that owns persistence,
  V: Triplication confirmed exactly as cited: OK-path saveState + snooze/silence clear + cancelAlarmNotification at thermostat_monitor.dart:640-660, thermostat_service.dart:156-175, and thermostat_service.dart:109-123; ThermostatFetchException/generic cat

* [low|bug|downgraded] _resolveNetworkWithToken constructs a new ThermostatHttpClient per call inside refreshHistory's loops and never closes any of them @app/lib/features/thermostats/data/thermostat_service.dart:388
  D: When the injected network lacks a token but the tokenSupplier resolves one, _resolveNetworkWithToken (thermostat_service.dart:388-397) creates a brand-new ThermostatHttpClient - i.e., two Dio instances with connection pools - on every invocation and never calls close() on it. refreshHistory calls it once per commit page (line 265) and once per sele
  R: Resolve the client once at the top of refreshHistory (and refresh) instead of per iteration, and close it in a finally block when it was locally constructed (the ThermostatHttpClient.close() API already exists for exactly this, per its doc comment). Longer term, delete the tokenS
  V: The mechanism is real: thermostat_service.dart:265 (per commits page) and :355 (per selected commit) call _resolveNetworkWithToken, and :392-395 constructs a new ThermostatHttpClient (two Dio instances) with no close() whenever _network lacks a token

* [medium|design-improvement|confirmed] refreshHistory is a 160-line god method mixing sampling policy, rate-limit strategy, and an LSP-violating downcast of the network abstraction @app/lib/features/thermostats/data/thermostat_service.dart:218
  D: ThermostatService.refreshHistory (thermostat_service.dart:218-380) interleaves four separable responsibilities: (1) backfill sampling policy - bucket intervals, per-run budgets, hash-stride sampling for >7d/>1y tails; (2) rate-limit strategy - request budget and inter-request delay chosen by token presence; (3) paging/orchestration against the comm
  R: Add 'bool get hasToken' to ThermostatNetworkDataSource and delete the downcast plus the Platform.environment probe from the service. Extract a pure HistoryBackfillPlanner: plan({now, newestLocal, oldestLocal, knownRevisionIds, commitsPage}) -> accepted commits, unit-tested agains

* [low|design-improvement|downgraded] Non-autoDispose family providers accumulate live Drift watch streams per visited thermostat and range for the app's lifetime @app/lib/features/thermostats/providers/thermostat_providers.dart:62
  D: thermostatSummaryProvider (:62-66), selectedHistoryRangeProvider (:70-73), and thermostatHistoryProvider (:75-95) are .family providers without .autoDispose. Every (thermostatId) or (thermostatId, range) combination ever watched creates a Drift query.watch() stream that stays subscribed forever after the page is popped - Riverpod only disposes fami
  R: Make thermostatSummaryProvider, thermostatHistoryProvider, and selectedHistoryRangeProvider autoDispose families. For selectedHistoryRangeProvider (which intentionally syncs the detail page and the fullscreen chart), keep it alive across the two pages with ref.keepAlive()/a link 
  V: The accumulation itself is confirmed: thermostat_providers.dart:62-66, 70-73, 75-95 are family providers without autoDispose, each thermostatHistoryProvider entry holds a live Drift watch (thermostat_database.dart:377-388) plus downsampling forever, 

* [low|design-improvement|downgraded] Views bypass the provider/service layer: pages construct HTTP clients and call background-infra functions directly @app/lib/features/settings/view/settings_page.dart:389
  D: Widgets reach around the DI graph into infrastructure: settings_page.dart:389 constructs a raw ThermostatHttpClient inside _testGithubToken; settings_page.dart:86, 190, 209 and thermostats_page.dart:195 call the top-level side-effecting initializeBackgroundMonitoring() (which opens its own ad-hoc ThermostatDatabase and re-registers WorkManager); se
  R: Introduce a MonitoringController (Riverpod Notifier or plain class behind a Provider) exposing setPollInterval/pauseFor/resume/testAlarm/testToken, which internally persists via AlertConfigRepository and reschedules via the background module. Views call only the controller; the t
  V: Every citation checks out: settings_page.dart:389 constructs ThermostatHttpClient in a widget handler (though it IS properly closed in a finally at :393-395), initializeBackgroundMonitoring() is called from widget callbacks at settings_page.dart:86, 

* [low|design-improvement|downgraded] settings_page.dart is a 1,026-line widget file with 15-callback prop drilling @app/lib/features/settings/view/settings_page.dart:409
  D: The settings feature has no providers of its own beyond three repository lookups; all behavior lives in the page. _SettingsPageState holds ~340 lines of async side-effect handlers, each repeating the same try/showSnackBar(humanizeError)/mounted pattern (at least 10 copies), and passes 15 separate callbacks into _SettingsContent (settings_page.dart:
  R: Split into per-section widgets (MonitoringSection, AlarmSection, ApiSection, DeveloperSection) that are ConsumerWidgets reading a SettingsController (AsyncNotifier) directly instead of receiving callbacks. Centralize the try/snackbar/humanizeError pattern in one helper (e.g., a r
  V: Facts verified: file is 1,026 lines; _SettingsContent declares exactly 15 callback fields (settings_page.dart:430-442); the try/catch-showSnackBar(humanizeError) pattern recurs ~10 times as cited. But roughly 600 of those lines are plain declarative 

* [low|bug|confirmed] thermostatNetworkProvider rebuilds the HTTP client on every token change but never closes the replaced client @app/lib/features/thermostats/providers/thermostat_providers.dart:33
  D: thermostatNetworkProvider constructs a ThermostatHttpClient (two Dio instances) and re-executes whenever _githubTokenProvider emits (loading -> token, or any config re-emit changing the token), but registers no ref.onDispose to close the previous client. Each rebuild strands the old Dio connection pools until idle-timeout. The database provider dir
  R: Add ref.onDispose(client.close) (guarding for the case where a custom dio was injected, which it never is here) in thermostatNetworkProvider, mirroring thermostatDatabaseProvider.

* [low|design-improvement|confirmed] ThermostatDatabase (including settings' AlertConfigEntries table) lives under features/thermostats/data, forcing cross-feature data coupling @app/lib/features/thermostats/data/thermostat_database.dart:135
  D: The single app-wide Drift database is filed under features/thermostats/data/ yet declares the settings feature's AlertConfigEntries table (:67-83) and its accessors (watchAlertConfig/getAlertConfig/updateAlertConfig/setLastMonitorRunAt, :405-442). As a result features/settings/data/alert_config_repository.dart:3 must import '../../thermostats/data/
  R: Move thermostat_database.dart (and its generated part) to core/db/ (or a top-level data/ package folder) as the shared infrastructure it already is; keep feature-specific DAOs/repositories in their feature folders importing core/db. Purely a file move plus import updates - no sch

* [low|design-improvement|confirmed] 403 anonymous-fallback request pattern duplicated four times in ThermostatHttpClient @app/lib/features/thermostats/data/thermostat_client.dart:174
  D: The 'issue authorized GET; on 403 with token and _allowAnonFallback, re-issue the same request on _dioNoAuth' dance is hand-copied in fetchHistory (:174-222), listCommits (:323-336), _fetchSnapshot (:505-519), and _resolveFileContent (:627-646), each with its own header set and status handling. The prior bug hunt found two of these copies had incon
  R: Extract a private Future<Response<String>> _getWithAnonFallback(String url, {Map<String,dynamic>? query, Map<String,dynamic>? headers}) implementing the auth-then-anon pattern once, and hoist token resolution to a single static resolveToken(String?) used once in the constructor. 

* [low|design-improvement|confirmed] Production-dead history API: fetchHistory (150 lines), replaceHistory, and the interface method they force onto every fake @app/lib/features/thermostats/data/thermostat_client.dart:150
  D: ThermostatNetworkDataSource.fetchHistory (:12) and its 150-line implementation (:150-295) have no production callers - the history path moved to listCommits + fetchRevisionValue (used by ThermostatService.refreshHistory). Likewise ThermostatRepository.replaceHistory (thermostat_repository.dart:298-325) is called only by its own unit test. Because f
  R: Delete fetchHistory from the interface and client (with its tests), delete replaceHistory, and keep database.listTemperatureReadings only if retained deliberately as a test helper (it is used by DB tests). Shrinking the interface to what production uses simplifies every fake.

* [low|design-improvement|confirmed] Injectable clock (nowProvider / clock params) exists but most data-layer writes still call DateTime.now() directly @app/lib/features/thermostats/data/thermostat_repository.dart:161
  D: The codebase established clock injection for testability - nowProvider (thermostat_providers.dart:166-169), clock params on ThermostatMonitorRunner (:585-595), AlertConfigRepository (:12), ThermostatHttpClient (:29) - but ThermostatRepository stamps every row with inline DateTime.now().toUtc() (saveState :161, create :67, update :92, recordOutOfRan
  R: Give ThermostatRepository the same optional 'DateTime Function() clock' constructor parameter AlertConfigRepository already has (wired from nowProvider in thermostatRepositoryProvider), and route view-layer 'now' reads through ref.watch(nowProvider). Mechanical change, no behavio

* [low|design-improvement|confirmed] thermostatHistoryRefreshProvider models a side-effect as a watched FutureProvider and uses ref.watch after async gaps @app/lib/features/thermostats/providers/thermostat_providers.dart:97
  D: The history refresh action is expressed as an autoDispose FutureProvider.family whose body sleeps 300ms, consults a hidden module-level throttle registry provider, then performs the network sweep; callers both watch it in build and ref.invalidate() it to re-trigger (thermostat_detail_page.dart:23-30, :44-49, :83-95). This works but is contorted: (a
  R: Convert to an AsyncNotifier (class HistoryRefresher extends AutoDisposeFamilyAsyncNotifier) with an explicit refresh({prioritizeLastHour}) method, resolving the service via ref.read at method start; the UI calls notifier.refresh() from onRefresh/onPressed and watches state for th

+ MISSED [low] buildMonitorDependencies constructs a ThermostatHttpClient every monitor run and never closes it — the most frequent instance of the lifecycle problem the reviewer flagged twice elsewhere @app/lib/core/background/thermostat_monitor.dart: Every background monitor run (default every 5 minutes, for the lifetime of the foreground-service isolate) calls buildMonitorDependencies (thermostat_monitor.dart:536), which constructs a fresh ThermostatHttpClient (:708) — two Dio instances with retry interceptors. _runMonitorTaskLocked's finally block closes only the database (:569); deps.network

+ MISSED [low] thermostatHistoryProvider freezes its 'since' cutoff at first build, so a retained chart's time window silently grows @app/lib/features/thermostats/providers/thermostat_providers.dart: thermostatHistoryProvider computes `since = DateTime.now().toUtc().subtract(window)` once in the provider body (thermostat_providers.dart:82-84) and uses that fixed instant both in the SQL query and the in-memory filter (:88-92) for the stream's entire life. Because the provider is a non-autoDispose family (the reviewer's own finding), the entry ca

+ MISSED [low] refreshHistory's request budget is decided by the injected client but requests may run on a different token-bearing client, so a supplier-resolved token still gets the anonymous budget @app/lib/features/thermostats/data/thermostat_service.dart: hasToken (and therefore perRunBudget 400-vs-20 and the 300ms inter-request delay) is computed from the injected _network at thermostat_service.dart:240-247, but the actual requests go through _resolveNetworkWithToken (:265, :355), which may return a brand-new client authenticated with the tokenSupplier's token (:392-395). In the token-stream loadin
SKEPTIC: A genuinely thorough review: all 13 findings are anchored to real code, line citations are almost entirely accurate, dead code was verified by search, and the reviewer correctly cross-checked .agent history (L-3, L-7, bug-hunt #1/#2) instead of re-reporting fixed issues. Its main weakness is inflati
============================================================
DIM: correctness — The codebase is in good shape for correctness in the areas past reviews hammered on: the alarm compare-and-set transaction, the fail-closed temperature parser, WAL + busy_timeout for the dual-isolate DB, and the monitor debounce/health machinery are all sound and well-commented. The remaining real bugs cluster in two under-reviewed areas: the history backfill pipeline (unbounded gist-commit pagination that can burn the entire GitHub rate limit and starve the alarm-path fetch, plus per-call HTTP-client leaks) and time-window handling in long-lived Riverpod providers (a frozen `since` cutoff on 

* [high|risk|confirmed] Silent sensor death is undetectable: gist data staleness is never checked and 'Updated X ago' reflects fetch time, not data time @app/lib/features/thermostats/data/thermostat_client.dart:119
  D: fetchCurrent parses the gist's current content and stamps the reading with `fetchedAt: _clock()` (the wall time of the HTTP fetch, thermostat_client.dart:131-135). The gist JSON's `updated_at` / latest commit time is never consulted. If the sensor-side script dies but the gist stays reachable, every poll succeeds, re-parses the last (possibly in-ra
  R: Extract the gist's `updated_at` (already present in the JSON fetched by _parseSnapshot) or the newest commit timestamp, store it as the reading's observation time separately from fetch time, drive 'Updated X ago' from it, and add a configurable staleness threshold (e.g. 3x poll i

* [high|bug|confirmed] refreshHistory pages through the gist's entire commit history every run once the local cache is saturated — no page cap; can exhaust the GitHub rate limit and break alarm fetches @app/lib/features/thermostats/data/thermostat_service.dart:264
  D: The selection loop `while (selected.length < perRunBudget)` (thermostat_service.dart:264-348) only exits when (a) the budget fills, (b) a page is empty, or (c) a page is shorter than 100. Commits that are already cached (`knownRevisionIds.contains(rev)` -> continue, line 279) or rejected by the bucket/stride filters (lines 303-336) do not count tow
  R: Cap pages per run (e.g. 3-5), and stop paging early once commit timestamps are older than both the oldest local reading and the 7-day stage-2 window unless stride sampling still has budget; alternatively stop as soon as a full page yields zero accepted commits.

* [medium|bug|confirmed] thermostatHistoryProvider freezes its `since` cutoff for the app session — history charts show an ever-growing window @app/lib/features/thermostats/providers/thermostat_providers.dart:82
  D: `thermostatHistoryProvider` is a non-autoDispose `StreamProvider.family`. Its body computes `since = DateTime.now().toUtc().subtract(window)` exactly once, when the (thermostatId, range) instance is first created, and the instance then lives for the rest of the app session (its only dependency, thermostatRepositoryProvider, never rebuilds). Every s
  R: Make the provider `autoDispose` so each page visit recomputes `since`, and/or recompute the cutoff per emission inside the `.map` (compute `since` from `ref.read(nowProvider)()` at map time and re-filter), optionally re-watching a periodic tick like offlineStatusProvider does.

* [low|bug|downgraded] ThermostatHttpClient instances are created per page/per revision in refreshHistory and never closed; thermostatNetworkProvider also leaks its client on rebuild @app/lib/features/thermostats/data/thermostat_service.dart:355
  D: `_resolveNetworkWithToken()` (thermostat_service.dart:388-397) constructs a brand-new `ThermostatHttpClient` (two Dio instances, each holding an HttpClient with persistent sockets) whenever `_network` lacks a token but the tokenSupplier returns one. refreshHistory calls it inside both the paging loop (line 265, once per page) and the revision-fetch
  R: Resolve the client once at the top of refreshHistory, reuse it for all pages/revisions, and close it in a `finally` when it was locally constructed; derive `hasToken`/budget from that resolved client. Add `ref.onDispose(client.close)` in thermostatNetworkProvider (requires exposi
  V: The code claims are all accurate: _resolveNetworkWithToken (thermostat_service.dart:388-397) news up an unclosed two-Dio client, called per page (line 265) and per revision (line 355); thermostatNetworkProvider (thermostat_providers.dart:33-41) has n

* [low|bug|downgraded] Alarm rate limit is bypassed when an error reading interleaves a continuing out-of-range excursion @app/lib/features/thermostats/data/thermostat_reading_utils.dart:32
  D: shouldTriggerAlarm applies the 5-minute `kAlarmRateLimit` only when `previousState.status == ThermostatReadingStatus.outOfRange` (lines 31-37). Error readings (networkError/httpError/parseError) overwrite `lastStatus` while leaving `lastAlarmAt` intact (thermostat_monitor.dart:662-680 saveState without setLastAlarmAt). Failure scenario with a 1-min
  R: Rate-limit on `lastAlarmAt` alone (drop the status==outOfRange condition), or treat error statuses as continuation of an excursion by tracking the last non-error status; keep the deliberate 'new excursion after OK' immediate re-alarm by resetting lastAlarmAt (or checking status==
  V: The mechanism is real and I could construct the failure: thermostat_reading_utils.dart:31-37 applies the rate limit only when previousState.status == outOfRange; the monitor's error path (thermostat_monitor.dart:662-670) saves the error status withou

* [low|bug|downgraded] Deleting a thermostat (or its row) with an active alarm never cancels the ongoing, non-dismissible alarm notification @app/lib/features/thermostats/view/thermostats_page.dart:160
  D: The alarm notification is posted with `ongoing: true` (thermostat_monitor.dart:822-823), so the user cannot swipe it away. `_deleteThermostat` calls `repository.delete(thermostat.id)` (thermostats_page.dart:160) which removes the DB rows (thermostat_repository.dart:113-119) but never calls `cancelAlarmNotification`. Subsequent monitor runs iterate 
  R: Call `cancelAlarmNotification(thermostat.id)` in the repository delete path (or in _deleteThermostat right after a successful delete), and cancel in the runner when skipping a monitoring-disabled thermostat whose previous status was outOfRange.
  V: Confirmed gap: thermostats_page.dart:157-182 delete flow and thermostat_repository.dart:113-119 delete are pure DB operations with no cancelAlarmNotification, the alarm is posted ongoing/autoCancel:false (thermostat_monitor.dart:822-823), and the mon

* [low|bug|confirmed] A failure while dispatching the alarm notification relabels a successful out-of-range reading as 'unknown' with a stale value @app/lib/core/background/thermostat_monitor.dart:671
  D: In ThermostatMonitorRunner.run, the generic `catch (error)` (line 671) wraps not just the fetch but also the post-fetch persistence and `_alarmDispatcher.showAlarm` call. If showAlarm throws (notification channel/platform error), the CAS transaction has already recorded status=outOfRange and advanced lastAlarmAt, and the catch then overwrites the r
  R: Narrow the try/catch: catch fetch errors around `_network.fetchCurrent` only, and guard the alarm dispatch with its own try/catch that logs without rewriting the just-persisted state.

* [low|ux-improvement|confirmed] Pause banner and relative 'Updated X ago' text freeze during a monitoring pause and can outlive the pause end @app/lib/features/thermostats/view/thermostats_page.dart:274
  D: ThermostatsPage.build computes `now = DateTime.now().toUtc()` and derives `pausedUntil` from it (lines 274-277). The page rebuilds only when a watched provider changes; during a pause the monitor writes no thermostat state (thermostat_monitor.dart:541-545), alertConfig doesn't change, and offlineStatusProvider's minute tick only notifies watchers w
  R: Watch a periodic tick (reuse the offline tick provider, exposed non-private) in ThermostatsPage/ThermostatCard, or schedule a rebuild timer for `pauseAllUntil` so the banner clears exactly at pause end.

* [low|risk|confirmed] Alarm notification ids can collide between thermostats (hash % 1,000,000), letting one alarm silently replace another @app/lib/core/background/thermostat_monitor.dart:873
  D: `_alarmNotificationId` maps the thermostat UUID to `4000 + (hashCode & 0x7fffffff) % 1000000`. Two thermostats whose id hashes collide modulo 1e6 share a notification id: if both go out of range, the second `show` replaces the first notification, so the user sees one alarm where there are two, and 'Acknowledge'/cancel on one also clears the other's
  R: Derive the id from a persisted per-thermostat integer (e.g. a monotonically assigned notificationId column) instead of a hash of the UUID, guaranteeing uniqueness and cross-isolate/restart stability.

* [medium|test-gap|confirmed] refreshHistory's selection/pagination logic is effectively untested @app/test/unit/thermostat_service_test.dart:457
  D: The only refreshHistory test ('refreshHistory persists revision samples', thermostat_service_test.dart:457) exercises a single small commit list. The riskiest logic — the `while (selected.length < perRunBudget)` paging loop, known-revision skipping, bucket/stride acceptance across the four time stages, budget accounting, and (per the pagination fin
  R: Add unit tests with a fake ThermostatNetworkDataSource returning multi-page commit lists: (a) all commits already cached -> assert bounded number of listCommits calls; (b) budget exhaustion mid-page; (c) stage-bucket acceptance at the 1h/24h/7d boundaries; (d) prioritizeLastHour 

+ MISSED [low] One failing history refresh aborts history refresh for all remaining thermostats @app/lib/core/background/thermostat_monitor.dart: In _runMonitorTaskLocked (thermostat_monitor.dart:547-555) a single try/catch wraps the entire per-thermostat history loop: `for (final summary in thermostats) { await service.refreshHistory(...); }`. The first thermostat whose refreshHistory throws (e.g. a 403 during the rate-limit storm from the pagination bug, or one misconfigured gist id) skips

+ MISSED [low] refreshHistory can never backfill gaps between the oldest and newest local readings @app/lib/features/thermostats/data/thermostat_service.dart: The selection logic (thermostat_service.dart:285-337) accepts a commit only if it is newer than newestLocal or older than oldestLocal; a commit strictly between the two that is not already cached leaves accept=false forever. After any coverage gap — e.g. the app is offline for a day, the next monitor run accepts only ~1 commit per 60-minute bucket 

+ MISSED [low] Dead code: ThermostatHttpClient.fetchHistory and ThermostatRepository.replaceHistory have no production callers @app/lib/features/thermostats/data/thermostat_client.dart: fetchHistory (thermostat_client.dart:150-295, ~145 lines including a duplicated 403-anon-fallback block) is declared on ThermostatNetworkDataSource (line 12) but grep shows no caller anywhere in app/lib — the history pipeline uses listCommits + fetchRevisionValue instead. Likewise ThermostatRepository.replaceHistory (thermostat_repository.dart:298-

+ MISSED [low] Background monitor refreshes history for monitoring-disabled thermostats @app/lib/core/background/thermostat_monitor.dart: ThermostatMonitorRunner.run skips thermostats with monitoringEnabled=false for the current-reading fetch (thermostat_monitor.dart:601-603), but the history loop right after it (lines 548-551) iterates repository.fetchThermostats() unfiltered, so a disabled thermostat still burns the full refreshHistory API budget (hundreds of requests per run given
SKEPTIC: A thorough, high-quality review: all ten findings are code-accurate with correct line citations, every failure scenario I attempted to construct actually reproduces, and the two headline findings (undetectable gist staleness and unbounded commit pagination) are genuine, novel, and well-argued — neit
============================================================
DIM: security — Security posture is unusually thoughtful for a hobby-scale Flutter app: the GitHub token has been migrated from the plaintext Drift DB into flutter_secure_storage with a careful read-back-before-scrub migration (alert_config_repository.dart), the developer log exporter deliberately omits the token, gist IDs are strictly regex-validated before URL interpolation, SQL is parameterized throughout, the temperature parser fails closed, and CI/release workflows handle signing secrets correctly with keystores/key.properties gitignored. The remaining issues are mostly robustness/availability rather tha

* [medium|risk|downgraded] Denied/revoked notification permission silently disables every alarm with no in-app warning @app/lib/core/permissions/notification_permission.dart:24
  D: ensureNotificationPermission() explicitly no-ops when POST_NOTIFICATIONS is permanently denied (status.isGranted || status.isPermanentlyDenied -> return) and is only called once at startup (main.dart:26). If the user denies the prompt or later revokes the permission, the OS suppresses the out-of-range alarm notifications, the full-screen alarm laun
  R: Add a persistent in-app banner (e.g. on the thermostats list and settings pages) when Permission.notification is not granted on Android 13+, with an 'Open settings' action (openAppSettings). Optionally have the monitor's health logic detect areNotificationsEnabled() via flutter_l
  V: Code claims all verified: app/lib/core/permissions/notification_permission.dart:24-26 returns without prompting on isPermanentlyDenied; app/lib/main.dart:26 is the sole call site; grep over app/lib finds no Permission.notification check in any UI (on

* [medium|bug|confirmed] fullScreenIntent is requested but USE_FULL_SCREEN_INTENT is not declared in the manifest @app/android/app/src/main/AndroidManifest.xml:2
  D: Alarm notifications are built with fullScreenIntent: true (thermostat_monitor.dart:779, default parameter at :762), intended to launch the full-screen alarm page over the lock screen. Since Android 10 (API 29) that requires the android.permission.USE_FULL_SCREEN_INTENT manifest permission; the manifest declares INTERNET/POST_NOTIFICATIONS/FOREGROUN
  R: Add <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/> to AndroidManifest.xml. On Android 14+, check NotificationManager.canUseFullScreenIntent() (exposed via flutter_local_notifications/permission_handler) and surface a settings prompt if revoked, since 

* [low|risk|downgraded] No response-size limit on gist/raw fetches — hostile or misconfigured gist content is buffered unbounded in memory @app/lib/features/thermostats/data/thermostat_client.dart:599
  D: _resolveFileContent follows the gist's raw_url when the API marks the file truncated (>1 MB) and reads the whole body into a String with ResponseType.plain (:615-654); _fetchSnapshot similarly buffers the full API JSON (:497). Dio's BaseOptions (:63-83) sets timeouts but no maxContentLength/response cap, and receiveTimeout only bounds inter-chunk g
  R: Set maxContentLength/maxRedirects on the Dio BaseOptions, or fetch raw content with ResponseType.stream and abort after a sane cap (e.g. 256 KB — a temperature reading is a few bytes). Only the head of the file is ever needed since parseCelsiusTemperature takes firstMatch.
  V: Code facts verified: _createDio (thermostat_client.dart:62-105) sets no maxContentLength, and _resolveFileContent (:615-654) buffers the whole raw_url body into a String. But the failure scenario is weak for this app's actual data model: the gist fil

* [medium|risk|confirmed] History backfill can burn the GitHub rate limit: up to 400 revision fetches per thermostat per poll, poll interval down to 1 minute, and 403s double as anonymous retries @app/lib/features/thermostats/data/thermostat_service.dart:249
  D: Every background monitor run calls refreshHistory() for every thermostat (thermostat_monitor.dart:548-551). With a token the per-run budget is 400 revision fetches plus commit-list paging (thermostat_service.dart:249, 264-348); the poll interval can be set as low as 1 minute (alert_config_repository.dart:99 clamps to 1..30). On a fresh install with
  R: Read X-RateLimit-Remaining/Reset from responses and skip history backfill (never the current-reading fetch) when the budget is low; add a cooldown after any 403 instead of instantly retrying anonymously; consider reserving anon-fallback for fetchCurrent only, since history is bes

* [low|risk|downgraded] Auto Backup not disabled: SQLite DB (with possible legacy plaintext token) is backed up, and restored secure-storage entries silently lose the token @app/android/app/src/main/AndroidManifest.xml:9
  D: The <application> element sets no android:allowBackup / android:dataExtractionRules, so Android Auto Backup (default on) uploads app data — including thermostats.sqlite — to the user's Google account. The alert_config_entries.github_token column still exists (thermostat_database.dart:80) and is only scrubbed lazily on loadConfig()/setGithubToken() 
  R: Set android:allowBackup="false" (or add dataExtractionRules excluding the databases and shared_prefs directories). This both prevents the legacy-token backup window and avoids the undecryptable-restore failure mode. Optionally surface a 'token missing' hint when secure storage re
  V: Manifest fact verified: <application> at AndroidManifest.xml:9-12 sets no allowBackup/dataExtractionRules, and the githubToken column still exists (thermostat_database.dart:80). But the 'backed up before the first post-upgrade read' window is far nar

* [low|bug|downgraded] ThermostatHttpClient instances are created per revision fetch / per provider rebuild and never closed @app/lib/features/thermostats/data/thermostat_service.dart:388
  D: _resolveNetworkWithToken() constructs a brand-new ThermostatHttpClient (two Dio instances each) whenever the injected _network lacks a token but the tokenSupplier yields one. It is called inside the commit-paging loop (:265) and once per selected revision inside the sample loop (:355) — up to ~400 unclosed clients (800 Dio/HttpClient instances, eac
  R: Resolve the client once at the top of refreshHistory (and refresh) and close it in a finally block if it was locally created; in thermostatNetworkProvider, use ref.onDispose to close the client when the provider rebuilds.
  V: The leak itself is real and verified: _resolveNetworkWithToken (thermostat_service.dart:388-397) constructs a new ThermostatHttpClient with no close(), called inside the paging loop (:265) and per selected revision (:355); thermostatNetworkProvider (

* [medium|design-improvement|confirmed] ETag is captured and persisted but never sent back — every poll re-downloads the full gist and burns rate limit @app/lib/features/thermostats/data/thermostat_client.dart:563
  D: _parseSnapshot extracts the response ETag (:563) and it is threaded through ThermostatFetchSuccess into saveState and the thermostat_state_entries.etag column (thermostat_database.dart:48), but no request ever sets If-None-Match. GitHub serves 304 responses to conditional requests without charging the authenticated rate limit, so the app is paying 
  R: Pass the stored etag into fetchCurrent and send If-None-Match; on 304, keep the previous value and update lastFetchedAt. This is the single cheapest way to make anonymous (60/hr) operation viable at short poll intervals.

* [low|ux-improvement|confirmed] Raw settings/alarm error surfaces bypass humanizeError and print raw exception text @app/lib/features/settings/view/settings_page.dart:1003
  D: error_messages.dart exists precisely because raw exception text 'can leak internal detail' (its own doc comment), and most paths route through humanizeError. But the settings load-failure widget renders error.toString() directly (settings_page.dart:1003), and ThermostatHttpClient.testToken's final catch returns 'GitHub auth error: $e' (thermostat_c
  R: Wrap both in humanizeError(error) (the settings _ErrorContent and the testToken catch-all), keeping the raw error in debugPrint only.

* [low|risk|confirmed] Token attached to raw_url fetch without a host allowlist, plus silent GITHUB_TOKEN env fallback @app/lib/features/thermostats/data/thermostat_client.dart:615
  D: _resolveFileContent sends 'Authorization: token …' to whatever raw_url string the gist API response contains (:615-626) with no check that the host is gist.githubusercontent.com. Today raw_url is generated by GitHub over TLS, so this is not exploitable, but it is one API quirk away from token exfiltration and costs one line to harden (modern Dart S
  R: Before attaching the Authorization header to raw_url, verify Uri.parse(rawUrl).host is api.github.com or gist.githubusercontent.com. Gate the GITHUB_TOKEN environment fallback behind kDebugMode or remove it in favor of FARMCTL_GITHUB_TOKEN only.

* [low|risk|confirmed] BootCompletedReceiver is exported and spins up a Flutter engine for any explicit intent with a matching action string @app/android/app/src/main/AndroidManifest.xml:41
  D: The receiver is android:exported="true" (required for BOOT_COMPLETED delivery), and its Kotlin onReceive only string-compares intent.action (BootCompletedReceiver.kt) — it does not verify the broadcast actually came from the system. BOOT_COMPLETED/MY_PACKAGE_REPLACED are protected broadcasts, so implicit spoofing is blocked, but any co-installed ap
  R: Keep exported=true (needed for boot) but this is acceptable as-is for the threat model; if hardening, check Build/UserHandle or simply accept — alternatively split MY_PACKAGE_REPLACED handling and add android:permission="android.permission.RECEIVE_BOOT_COMPLETED" isn't applicable

* [low|risk|confirmed] Release-critical GitHub Actions pinned by mutable tag, not commit SHA @.github/workflows/release.yaml:80
  D: release.yaml runs with contents:write permission and handles the Android signing keystore + passwords, but uses actions/checkout@v4, subosito/flutter-action@v2 and softprops/action-gh-release@v2 pinned by tag. A compromised or re-pointed tag in any of these third-party actions could exfiltrate ANDROID_KEYSTORE_BASE64/passwords or tamper with the pu
  R: Pin the three actions in release.yaml to full commit SHAs (with a comment noting the version), and consider adding a Dependabot 'github-actions' ecosystem entry to keep the pins fresh.

* [low|risk|confirmed] Watchdog cannot restart a dead monitoring service while the device is offline @app/lib/core/background/thermostat_monitor.dart:286
  D: The WorkManager watchdog — the only mechanism that revives the foreground service after an OEM battery manager kills the whole app process — is registered with Constraints(networkType: NetworkType.connected). On a farm with patchy connectivity, a process kill during an outage leaves monitoring dead until connectivity returns AND the next 15-minute 
  R: Drop the network constraint from registerPeriodicTask so the watchdog can restart the service (and its ongoing notification / failure counter) regardless of connectivity; fetch failures inside the run already handle offline correctly.

* [low|test-gap|confirmed] No regression test guarding that the developer log export and UI surfaces never contain the GitHub token @app/lib/features/settings/services/developer_log_exporter.dart:41
  D: DeveloperLogExporter currently serializes only safe fields (config poll/sound/vibrate/pause and thermostat summaries — no githubToken), which is correct. But the payload is hand-assembled field-by-field, and AlertConfig carries the token right next to the serialized fields; a future 'just dump the config' refactor would silently start writing the P
  R: Add a unit test that exports with a config containing a sentinel token (e.g. 'ghp_SENTINEL') and asserts the produced JSON string does not contain it; same cheap assertion could cover humanizeError output for a DioException built with an Authorization header.

* [low|risk|confirmed] Released app ships with placeholder applicationId com.example.farmctl @app/android/app/build.gradle.kts:39
  D: The signed release APK uses applicationId com.example.farmctl. Beyond looking unfinished, com.example.* namespaces are squattable: if the app is ever distributed beyond direct APK sideloads, another party publishing that ID on a store would conflict, and the ID can never be changed later without users losing data (new ID = new app, secure-storage a
  R: If any wider distribution is ever intended, rename to an owned domain (e.g. dev.<name>.farmctl) now, while the install base is one device — it is a breaking change that only gets more expensive.

+ MISSED [low] Developer log export discloses gist IDs, which for secret gists are the access capability @app/lib/features/settings/services/developer_log_exporter.dart: The export the user is expected to share for diagnostics serializes 'gistId': thermostat.rawUrl (developer_log_exporter.dart:75). A secret gist is protected only by its unguessable ID, so the diagnostics file hands out permanent read access to the farm's telemetry source (and, via the gist API, the owner's GitHub identity). It also includes statusM

+ MISSED [low] Raw exception text is persisted into DB statusMessage and surfaces on thermostat cards and in the log export @app/lib/core/background/thermostat_monitor.dart: The catch-all paths write 'Unexpected error: $error' into the thermostat state message (thermostat_monitor.dart:678 and thermostat_service.dart:200), which is rendered on the thermostat card UI and serialized into the developer log export (developer_log_exporter.dart:90). This is the same humanizeError-bypass hygiene violation as the reviewer's fin

+ MISSED [low] A revoked/expired GitHub token is permanently masked by the anonymous 403 fallback with no user surfacing @app/lib/features/thermostats/data/thermostat_client.dart: Every fetch path silently falls back to the unauthenticated client on any 403 (thermostat_client.dart:174-185, 323-335, 505-517), including 403s caused by a revoked, expired, or SSO-blocked token rather than rate limiting. The app then runs indefinitely at the 60/hr anonymous limit while Settings still shows a saved token and nothing tells the user
SKEPTIC: This was a genuinely thorough, code-grounded review: all 14 findings checked out factually — every cited file, line, and mechanism matched the current code, none had been already fixed per the .agent/ history, and the reviewer clearly read the migration/scrub logic and CI workflows rather than patte
============================================================
DIM: testing — The test suite is unusually strong for a hobby-scale Flutter app: unit tests use a real in-memory Drift DB rather than mocks, clocks are injected (nowProvider, clock params) so time logic is deterministic, the parser has adversarial fail-closed cases, migrations are tested across v1/v6/v7/v8 upgrade paths, and most gaps flagged in the 2026-06-27 internal review (hysteresis, alarm rate-limit matrix, client 403 fallback, exporter) have since been closed. The remaining weakness is concentrated exactly where a bug would hurt most: the background delivery path. thermostat_monitor.dart sits at 22% l

* [high|test-gap|confirmed] Monitor run orchestration (_runMonitorTaskLocked) is entirely untested — the file is at 22% coverage @app/lib/core/background/thermostat_monitor.dart:487
  D: The pure helpers (shouldSkipMonitorRun, effectiveServiceInterval, nextMonitorHealth, pollIntervalMillis) each have thorough tests, but the orchestration that consumes them — the code that actually runs in the background isolate on every poll — has zero coverage. Untested behaviors include: (1) an active pause skips runner.run() but still executes p
  R: Extract the body of _runMonitorTaskLocked into a testable class (e.g. MonitorRunCoordinator) that takes the database, an AlertConfig loader, and a small ServiceCadence/Notification facade as constructor injections; keep _runMonitorTaskLocked as a thin shell that wires real implem

* [high|test-gap|confirmed] Notification action handler (_handleNotificationResponse) and cold-launch tap routing are untested @app/lib/core/background/thermostat_monitor.dart:946
  D: The path a user takes to snooze or silence an alarm from the lock-screen notification — potentially at 3am with the app terminated — has no test. Untested behaviors: malformed/empty payload JSON is tolerated without crashing; a body tap navigates to the alarm screen and cancels the notification; a snooze action writes snoozedUntil = now + duration;
  R: Refactor _handleNotificationResponse to accept an injectable database factory (defaulting to ThermostatDatabase()) so a test can pass an in-memory DB, then test: valid snooze payload → snoozedUntil persisted at now+5/10/30min; silence action → silenceUntilOk=true and snoozedUntil

* [medium|test-gap|confirmed] Alarm page action buttons are rendered but never tapped; PopScope test asserts an implementation detail @app/test/widget/alarm_fullscreen_page_test.dart:67
  D: alarm_fullscreen_page_test.dart verifies the page renders labels and that a PopScope(canPop:false) widget exists, but no test taps Acknowledge, Snooze 5/10/30, or Silence to assert the user-visible outcome: the repository write (updateSnoozedUntil / updateSilenceUntilOk), the notification cancel, and the page popping. The PopScope test (line 67-84)
  R: Override thermostatRepositoryProvider with a recording fake (or the real repository on an in-memory DB, as other tests do), pump the page inside a Navigator, tap each action, and assert: 'Snooze 5 minutes' → updateSnoozedUntil called with a value 5 minutes ahead (pin time by rout

* [medium|risk|confirmed] main.dart and notification_permission.dart are invisible to the 70% coverage gate @app/lib/core/permissions/notification_permission.dart:16
  D: flutter test --coverage only reports files loaded by the test bundle. lib/main.dart and lib/core/permissions/notification_permission.dart are imported by no test, so they do not appear in app/coverage/lcov.info at all — tool/check_coverage.sh (which sums LH/LF over the file) never sees them, and whole new untested files added the same way would als
  R: Two parts: (1) add a unit test for ensureNotificationPermission using permission_handler's PermissionHandlerPlatform mock (set the mock platform instance, stub checkPermissionStatus/requestPermissions) asserting request is called only for the undecided status and that a platform 

* [medium|test-gap|confirmed] Settings page has render-only tests — none of its interactions are exercised (42.7% coverage) @app/test/widget/settings_page_test.dart:44
  D: settings_page.dart is the largest view in the app (1026 lines) and its five tests only assert that sections render and that Resume is enabled/disabled by the pause state. Untested user flows: choosing a poll interval (persists via repository and reconfigures the foreground service), Pause monitoring for each duration, tapping Resume (clearPause), p
  R: Add interaction tests following the pattern of thermostats_page_interaction_test.dart: override alertConfigRepositoryProvider (or back it with an in-memory DB plus a fake SecureTokenStore) and soundPickerProvider, then tap through each control and assert the persisted config chan

* [medium|test-gap|confirmed] Router and deep-link handling have no navigation tests (44.1% coverage) @app/lib/core/router/app_router.dart:13
  D: No test drives the real GoRouter. Untested: the /alarm/:id root-level route (the target of every notification tap), the detail route param extraction, the history-fullscreen route's ?range= query parsing with fallback to ThermostatHistoryRange.day on an unknown value (app_router.dart:70-73), the errorBuilder _RouteNotFoundPage for a stale/malformed
  R: Add a router test file that builds the app via appRouterProvider inside a ProviderScope with DB/config overrides (reuse widget_test.dart's buildApp), then: router.go('/alarm/some-id') → AlarmFullScreenPage visible above the shell; router.go('/thermostats/detail/x/history?range=we

* [medium|design-improvement|confirmed] Foreground-service lifecycle and watchdog logic are untestable as written and untested @app/lib/core/background/thermostat_monitor.dart:146
  D: _ensureForegroundServiceRunning (running → updateService, cold → init+startService, ServiceRequestFailure → false), _runWatchdogTask (dead service → reload config → restart, propagating failure so WorkManager retries with backoff), and initializeBackgroundMonitoring all call FlutterForegroundTask/Workmanager statics directly, so none of their branc
  R: Introduce a minimal ForegroundServiceGateway interface (isRunning, update(options), start(...), init(...)) plus a WatchdogScheduler wrapper for Workmanager, injected into these functions (default = real plugin implementations). Then unit-test the decision logic: service running →

* [low|design-improvement|downgraded] Direct DateTime.now() calls bypass the nowProvider seam, leaving time-dependent branches untested @app/lib/features/thermostats/view/alarm_fullscreen_page.dart:149
  D: The codebase deliberately provides nowProvider ('Override in tests to pin now') and uses it in foreground_refresher and offlineStatusProvider, but three time-sensitive spots call DateTime.now() directly: (1) _AlarmContentState._elapsedText (alarm_fullscreen_page.dart:149) — the 'Out of range for X' line is consequently never asserted in any test (n
  R: Route all three through ref.read(nowProvider)() (the alarm page widgets are already ConsumerWidgets/ConsumerStates so this is a one-line change each). Then add: an alarm-page test with lastAlarmAt set and nowProvider pinned asserting the exact 'Out of range for 42 minutes' text; 
  V: All three citations verified exact: alarm_fullscreen_page.dart:149 (_elapsedText), :301 (_setSnooze), thermostat_providers.dart:83 (history since), versus nowProvider at thermostat_providers.dart:166-169 used at :114 and :188; thermostat_history_prov

* [medium|test-gap|confirmed] Monitor runner has no multi-thermostat isolation test (one thermostat's persistence failure aborting the sweep) @app/lib/core/background/thermostat_monitor.dart:597
  D: Every ThermostatMonitorRunner test uses exactly one thermostat. The per-thermostat try/catch inside run() catches fetch errors, but the catch handlers themselves call _repository.saveState (:663, :672) — if that throws (DB locked, disk full), the exception escapes the loop and every remaining thermostat is skipped for that cycle, meaning thermostat
  R: Add a runner test with two monitored thermostats where the first fetch throws a plain Exception and the second returns an out-of-range value; assert the second thermostat's alarm still fires. Then decide the saveState-failure contract: either wrap each loop iteration in an outer 

* [low|test-gap|confirmed] thermostatHistoryRefreshProvider's debounce, throttle, and stamp-on-success logic untested @app/lib/features/thermostats/providers/thermostat_providers.dart:97
  D: The refresh provider implements three behaviors with comments explaining their intent, none directly tested: a 300ms debounce that cancels if the provider is disposed (quick tab flips), a 10s per-thermostat throttle keyed off nowProvider, and stamping lastRun only after a successful refresh so a failure doesn't lock the user out of retrying for 10s
  R: Add a provider unit test using ProviderContainer with thermostatServiceProvider overridden by a counting/throwing fake and nowProvider pinned: (1) two reads within 10s → service.refreshHistory called once; (2) advance pinned now by 11s → called again; (3) make the first call thro

* [low|test-gap|confirmed] FlutterSecureTokenStore has 0% coverage — its normalization rules are untested @app/lib/features/settings/data/secure_token_store.dart:14
  D: alert_config_repository_test.dart thoroughly tests token handling but always through a fake SecureTokenStore, so the real FlutterSecureTokenStore is never executed (lcov: 0/8 lines). Its contract — empty string read normalizes to null, and writeToken(null or '') deletes the key rather than storing an empty value — is exactly the kind of edge behavi
  R: Add a small unit test constructing FlutterSecureTokenStore with a mocked FlutterSecureStorage (constructor already accepts one): read returning '' → null; read returning 'ghp_x' → 'ghp_x'; writeToken(null) and writeToken('') → delete(key: 'github_token') called, write never calle

* [low|test-gap|confirmed] No golden tests for the history chart — rendering regressions are invisible to the suite @app/test/widget/thermostat_history_chart_test.dart:63
  D: The chart tests are good behavioral smoke tests (empty state, single sample, flat series, every range's axis formatter, wide-span fallback) but assert only that rendering does not crash and that a semantics summary exists. Nothing pins what the chart looks like: axis label formatting per range, the min/max threshold band placement, line color vs. o
  R: Add golden tests (matchesGoldenFile with a pinned font via golden_toolkit or loaded Roboto) for a small matrix: day range with a normal series crossing the max threshold, week range flat series, and the empty state — in light and dark themes. Keep the set small (4-6 goldens) to l

+ MISSED [low] History fullscreen page tests are render-only and skip the portrait layout branch (63.9% coverage) @app/test/widget/thermostat_history_fullscreen_page_test.dart: All three tests pump only the landscape branch (900x480 surface, noted in the test's own comment at line 60) and never interact: the range dropdown is found but never opened/changed, so _setRange writing back to selectedHistoryRangeProvider (thermostat_history_fullscreen_page.dart:42-44) and _refreshHistory's invalidate-then-await sequence (:47-58)

+ MISSED [low] AlertConfigRepository write paths setSoundUri, setVolumeBoost, and pauseFor are untested even at the unit level @app/lib/features/settings/data/alert_config_repository.dart: lcov shows alert_config_repository.dart at 73.1% with DA:105-107 (setSoundUri), DA:117-121 (setVolumeBoost), and DA:123-125 (pauseFor) unhit — so these setters have no coverage at any layer, compounding the settings-page finding: the UI never taps them AND no unit test exercises them against the in-memory DB that alert_config_repository_test.dart a
SKEPTIC: Unusually rigorous review: every coverage figure (22.2%, 42.7%, 44.1%, 0/8, 78.8%, 84.8%), line citation, and test-file characterization I checked matched the actual code exactly, including subtle claims like the PopScope test being a widget-predicate tautology and the lcov SF list missing main.dart
============================================================
DIM: ux — Post-"Comprehensive UX overhaul" (PR #26/#27) the day-to-day UX is genuinely strong: every screen has distinct loading/empty/error states with retry, errors are humanized via a central helper, status is never color-only, semantics labels are pervasive, and the alarm page has well-ordered, large, escalation-aware actions. The remaining gaps cluster around the thing the farmer must trust most — the alarm delivery chain: the "alarm" is a once-per-post notification sound rather than a persistent alarm-clock ring, two alarm settings (Boost volume, and effectively Vibrate) do not do what their label

* [high|bug|confirmed] Boost volume setting is a complete no-op @app/lib/features/settings/view/settings_page.dart:631
  D: Settings offers a 'Boost volume' switch whose subtitle promises 'Keeps alarm volume at maximum while alarming.' The value is persisted (alert_config.dart:20, thermostat_database.dart:76) and exported in the developer log, but nothing in the alarm dispatch path ever reads it: thermostat_monitor.dart's _prepareAlarmNotificationDetails (lines 756-793)
  R: Either implement it (e.g. use an audio-manager plugin to raise STREAM_ALARM volume when posting an alarm notification and restore it after acknowledge) or remove the switch entirely. A setting that silently does nothing is worse than no setting; at minimum hide it until implement

* [high|ux-improvement|confirmed] Alarm sounds once per notification, not like an alarm clock; spec requires alarm-app-like persistence @app/lib/core/background/thermostat_monitor.dart:773
  D: Spec 3.5 requires 'audible alarms (Android alarm-app-like behavior)' and 7.2E says the alarm surface 'Plays chosen sound at alarm stream'. The implementation posts a high-importance notification on an alarm-usage channel; Android plays the channel sound once per post, then goes silent. Re-alarm happens only after the 5-minute rate limit (thermostat
  R: Add the Android FLAG_INSISTENT so the sound loops until the notification is acknowledged: AndroidNotificationDetails(..., additionalFlags: Int32List.fromList([4])). Grep confirms additionalFlags/insistent appear nowhere in the app. Optionally make loudness/persistence a per-therm

* [high|risk|confirmed] Denied notification permission silently kills all alarms with no in-app warning @app/lib/core/permissions/notification_permission.dart:24
  D: ensureNotificationPermission() deliberately no-ops when the permission is permanently denied, and main.dart:26 just awaits it. The doc comment itself states that without POST_NOTIFICATIONS 'the app's core safety alert would never reach the user'. Yet no screen reflects this state: the thermostats page shows pause and offline banners (thermostats_pa
  R: Add a persistent, error-styled banner on the thermostats page (mirroring _PauseBanner) plus a Settings tile when Permission.notification is denied/permanently denied, with an 'Open settings' action (openAppSettings) — the exact pattern already used for battery optimisation. The '

* [high|ux-improvement|confirmed] Snoozed/silenced state is invisible outside the alarm screen and cannot be reversed @app/lib/features/thermostats/widgets/thermostat_card.dart:41
  D: snoozedUntil and silenceUntilOk are persisted per thermostat and fully suppress alarms (thermostat_reading_utils.dart:22-29), but they render only inside AlarmFullScreenPage (alarm_fullscreen_page.dart:254-262). ThermostatCard and the detail page (which just embeds the card) show nothing — a thermostat silenced-until-OK at 3am from a notification a
  R: Add a muted/snoozed indicator to ThermostatCard (e.g. a notifications_off status chip with 'Snoozed until 07:30' / 'Silenced until back in range') driven by state.snoozedUntil/silenceUntilOk, include it in the card's semantics value, and add 'Resume alerts' / snooze quick actions

* [medium|bug|confirmed] Vibrate toggle is likely ineffective: alarm channel hard-codes enableVibration true and is never recreated @app/lib/core/background/thermostat_monitor.dart:752
  D: On Android 8+ (minSdk is 26) vibration is controlled by the notification channel, not the per-notification flag. _buildAlarmChannel creates the alarm channel with enableVibration: true unconditionally (line 752), and the channel id depends only on the sound URI (_alarmChannelIdForSound, lines 727-733), so toggling 'Vibrate on alarm' off in Settings
  R: Fold config.vibrate into the channel identity (e.g. suffix the channel id with the vibrate flag, like the sound hash) so flipping the switch creates a channel with the correct setting, or delete-and-recreate the channel when the setting changes. Add a widget/unit test asserting t

* [medium|ux-improvement|confirmed] Alarm page 'Out of range for …' resets every re-alarm, understating the excursion duration @app/lib/features/thermostats/view/alarm_fullscreen_page.dart:144
  D: _elapsedText computes 'Out of range for X' from state.lastAlarmAt. But lastAlarmAt is advanced to 'now' on every alarm fire (thermostat_repository.dart:232, fire ? drift.Value(now)), and while a reading stays out of range the monitor re-fires every 5 minutes (kAlarmRateLimit) unless snoozed/silenced. So a barn that has been freezing for 6 hours sho
  R: Track the excursion start separately: persist an outOfRangeSince timestamp set when status transitions into outOfRange (and cleared on OK) in recordOutOfRangeAndShouldAlarm, and compute the elapsed text from that instead of lastAlarmAt.

* [medium|ux-improvement|confirmed] Per-thermostat monitoring and hysteresis toggles exist in the data model but have no UI @app/lib/features/thermostats/widgets/thermostat_form_dialog.dart:226
  D: Spec 3.6 requires per-thermostat 'enable/disable monitoring', 7.2A a Monitor on/off toggle on each card, 7.2B Hysteresis and Monitoring switches on the Add/Edit form, and 3.2 a hysteresis 'UI toggle'. The model, DB, and monitor all honor these fields (thermostat.dart:24-25; monitor skips !monitoringEnabled at thermostat_monitor.dart:601; hysteresis
  R: Add a 'Monitoring' switch to ThermostatFormDialog (and ideally a quick toggle on the card per spec 7.2A), plus a Hysteresis switch with a one-line explanation. Show a visible 'Monitoring off' status on cards for disabled thermostats so they aren't mistaken for armed ones.

* [medium|ux-improvement|confirmed] 'Updated X mins ago' and the stale marker freeze while the screen is open @app/lib/features/thermostats/widgets/thermostat_card.dart:52
  D: ThermostatCard computes now/isStale/relative text at build time (lines 52-56, 214-217) and rebuilds only when a DB row changes (thermostatsProvider stream) — there is no per-minute ticker driving the cards. The existing minute tick (_offlineRefreshTickProvider, thermostat_providers.dart:174-176) feeds only offlineStatusProvider, a Provider<OfflineS
  R: Drive card rebuilds from a shared 1-minute ticker (e.g. watch a periodic StreamProvider in ThermostatsPage/ThermostatCard) so relative times and the stale flag recompute on wall-clock time. Consider deriving the stale threshold from the configured poll interval (e.g. 3x pollInter

* [low|ux-improvement|confirmed] Settings failure screen dumps a raw exception instead of the humanized message @app/lib/features/settings/view/settings_page.dart:1003
  D: The UX overhaul routed every error surface through humanizeError() specifically to avoid raw Dio/Drift exception text, but _ErrorContent on the Settings page still renders Text(error.toString()) — the one remaining screen-level surface that can show 'DriftRemoteException: …' to a farmer. It also has no Retry action, unlike the thermostats-page _Err
  R: Use humanizeError(error) and add a Retry button that invalidates alertConfigProvider, matching the _ErrorState pattern on the thermostats page.

* [low|ux-improvement|confirmed] Alarm sound setting shows a raw content:// URI as its label @app/lib/features/settings/view/settings_page.dart:600
  D: After picking a custom alarm sound, the 'Alarm sound' tile subtitle displays widget.config.soundUri verbatim — e.g. 'content://media/external/audio/media/1234?title=…' — which is meaningless and looks broken to a non-technical user. The system picker knows the ringtone's human title, but only the URI is persisted.
  R: Persist the picked sound's display title alongside the URI (SoundSelection can carry it from RingtoneManager) and show 'Rooster crow' instead of the URI; fall back to 'Custom sound' when the title is unavailable.

* [low|ux-improvement|confirmed] Card exposes the internal DB UUID as an 'Identifier' chip @app/lib/features/thermostats/widgets/thermostat_card.dart:250
  D: Each card renders a _MetaChip labelled 'Identifier' containing thermostat.id — the app-generated UUID (e.g. '3f8a1c…'), not the Gist ID the user entered (that already appears as the card subtitle at line 104). The UUID is developer noise: a farmer can do nothing with it, it consumes a full chip row on every card, and at a glance it looks like confi
  R: Drop the Identifier chip (or move the value to the developer-log export only). If a visible reference is wanted, show the Gist ID chip instead — it is what the user actually configured.

* [low|feature-idea|confirmed] No diagnostics/event-log surface (spec 3.7/7.2C): only the single last message survives @-
  D: Spec 3.7 calls for a diagnostics screen with the last 50 events per thermostat, and 7.2C for 'last fetch status, last 10 log lines' on Details. The app persists only one statusMessage per thermostat (overwritten each run), so intermittent failures — e.g. rate-limiting every other poll, or a sensor that flaps between OK and parse errors — are invisi
  R: Add a small EventLog table (capped, as spec 9 describes) written by the monitor, and render the last ~10 entries in an expandable 'Recent activity' section on the detail page. This also makes 'why didn't it alarm last night?' answerable — the single most important trust question.

* [low|ux-improvement|confirmed] History chart has tooltips only — no pan/zoom, and no min/avg/max in aggregated tooltips @app/lib/features/thermostats/widgets/thermostat_history_chart.dart:186
  D: Spec 7.2C specifies 'Interactive pan/zoom; data point tooltip'. The chart implements touch tooltips but no pan or zoom, so on a 30D/1Y/All range (2-hour buckets, hundreds of points on a phone width) the farmer cannot zoom into the night the temperature dipped. Additionally, buckets average their samples (thermostat_history_downsampler.dart:90) and 
  R: Track min/max per bucket in _SampleBucket and show 'avg 7.2 (min 5.9 / max 8.1)' in the tooltip; consider fl_chart's transformation/zoom support or a drag-to-select-window interaction for long ranges.

* [low|ux-improvement|confirmed] No search on the thermostats list (spec 7.2A) @app/lib/features/thermostats/view/thermostats_page.dart:301
  D: Spec 7.2A specifies 'Header: Thermostats + search'. The AppBar has only the title; with the tall card layout (~280px each, mainAxisExtent at line 260) a farm with 10+ sensors means significant scrolling to find one barn, and there is no sort/filter (e.g. problems first) either. Cards are not ordered by severity, so an out-of-range thermostat can si
  R: Lowest-cost high-value fix: sort the list problems-first (danger > warning/offline > ok) so anything alarming is always at the top; add a search field or SearchAnchor in the AppBar once herds of sensors are realistic.

* [low|ux-improvement|confirmed] Delete Undo does not warn that reading history is unrecoverable @app/lib/features/thermostats/view/thermostats_page.dart:164
  D: The delete confirmation dialog says only 'Delete "name"?' and the snackbar 'Thermostat deleted' with Undo. The code comments acknowledge that deletion drops all reading history and that Undo restores only the configuration — but neither fact is told to the user. A farmer who deletes and immediately undoes believes everything is back, yet a year of 
  R: Say it in the UI: confirm dialog 'Delete "name"? Its temperature history will be permanently removed.' and, after an Undo, a follow-up snackbar 'Thermostat restored (history could not be recovered)'.

* [low|ux-improvement|confirmed] Alarm notification body uses 2-decimal temperatures, inconsistent with the 1-decimal UI @app/lib/core/background/thermostat_monitor.dart:834
  D: The alarm notification shows 'Current 2.13°C • Range 4.00°C – 10.00°C' (toStringAsFixed(2)), and the persisted out-of-range statusMessage does the same (formatOutOfRangeThermostatMessage), while every UI surface (card hero, alarm page, chart) standardized on one decimal in the UX overhaul. The status-detail line on the card therefore mixes precisio
  R: Standardize on toStringAsFixed(1) in showAlarm's notification body and formatOutOfRangeThermostatMessage (and the 'Fetched X°C' OK message at thermostat_monitor.dart:641) to match the app-wide 1-dp convention.

+ MISSED [medium] 'Last update' timestamp advances on failed fetches, so the hero misstates the reading's age and the '(stale)' badge is unreachable while the service is alive @app/lib/features/thermostats/widgets/thermostat_card.dart: Both the background monitor's error paths (thermostat_monitor.dart:662-679) and the foreground service refresh error path (thermostat_service.dart:182-189) write lastFetchedAt := now on FAILED fetches while carrying forward the old lastValueC. The card hero then shows the hours-old last-known-good temperature next to 'Last update: 2 mins ago' (ther

+ MISSED [low] Raw exception text can reach the card via the catch-all statusMessage @app/lib/core/background/thermostat_monitor.dart: The monitor's catch-all writes message: 'Unexpected error: $error' (thermostat_monitor.dart:678), and thermostat_service.dart has an equivalent path. That string is persisted as statusMessage and rendered verbatim in the card's 'Last message' _MetaChip (thermostat_card.dart:255-261) and in the status footer detail (thermostat_status_presentation.da

+ MISSED [low] Dead audio dependencies (just_audio, audio_session) declared but never imported @app/pubspec.yaml: app/pubspec.yaml:29-30 declares audio_session ^0.2.2 and just_audio ^0.10.5, but grep shows zero imports of either anywhere in app/lib. They appear to be scaffolding for the never-implemented 'Boost volume' / in-app alarm playback (reviewer's findings 1 and 2) that was abandoned. They add APK size and native plugin registration for nothing, and mis

+ MISSED [low] No About section in Settings (spec 7.2D: version, licenses, privacy) @app/lib/features/settings/view/settings_page.dart: docs/Spec.md:246 specifies an About block (version, licenses, privacy) under Settings. The page has four _Section blocks (settings_page.dart:500,591,656,748 — Monitoring, Alarm, API configuration, developer/logs) and no about/version/licenses anywhere (grep for 'About'/'Licenses' returns nothing). Minor for a single-user app, but a version string i
SKEPTIC: This is an unusually accurate review: all 16 findings survived verification against the current code, every cited file/line checked out (or was off by only a couple of lines), and severities are honestly calibrated — spec-mandated but low-value-for-one-farmer gaps (search, diagnostics, pan/zoom) wer
============================================================
DIM: features — FarmCtl's core loop — Gist-backed readings, a foreground-service watchdog with a WorkManager restart net, per-thermostat snooze/silence with transactional alarm arbitration, and a polished list/detail/chart UI — is genuinely strong and shows several rounds of reliability hardening. The remaining product gaps cluster around end-to-end alarm trust: the full-screen alarm cannot actually appear on modern Android because the manifest never declares USE_FULL_SCREEN_INTENT, the 'Boost volume' setting is a no-op, a dead sensor that stops publishing to its gist is reported as green/OK forever, and ther

* [high|bug|downgraded] Full-screen alarm cannot appear: USE_FULL_SCREEN_INTENT permission is never declared @app/android/app/src/main/AndroidManifest.xml:8
  D: The alarm notification is built with fullScreenIntent: true (app/lib/core/background/thermostat_monitor.dart:779), and the whole alarm surface (alarm_fullscreen_page.dart) is designed as the primary 'wake the farmer' UX. But AndroidManifest.xml declares only INTERNET, POST_NOTIFICATIONS, FOREGROUND_SERVICE(_SPECIAL_USE), REQUEST_IGNORE_BATTERY_OPTI
  R: Add <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/> to the manifest; on Android 14+ check canUseFullScreenIntent at startup/settings and deep-link to ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT when revoked (surface it in the alarm-health check proposed s
  V: Factually verified: app/android/app/src/main/AndroidManifest.xml:2-8 declares only INTERNET, POST_NOTIFICATIONS, FOREGROUND_SERVICE(_SPECIAL_USE), REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, RECEIVE_BOOT_COMPLETED, WAKE_LOCK; I also checked the flutter_loc

* [critical|feature-idea|confirmed] A dead sensor never alarms: no stale-data ('sensor went silent') detection @app/lib/core/background/thermostat_monitor.dart:597
  D: The monitor only raises an audible alarm on an out-of-range temperature (ThermostatMonitorRunner.run, thermostat_monitor.dart:597-683). Two silent-failure modes get no alarm: (1) the sensor/uploader dies but the gist stays readable — fetchCurrent succeeds with the old content, fetchedAt is set to device receive time, and the thermostat shows fresh 
  R: Add a per-thermostat 'alert when no new reading for N minutes' (default ~3x expected sensor cadence, off = never). In the monitor run, compare the latest gist revision timestamp (or gist updated_at, one cheap field on the response already being fetched) against now; when exceeded

* [high|bug|confirmed] 'Boost volume' setting is a dead toggle — alarm audibility depends entirely on the phone's alarm-stream volume @app/lib/features/settings/view/settings_page.dart:634
  D: Settings exposes a 'Boost volume' switch (settings_page.dart:632-647) persisted to AlertConfig.volumeBoost, but nothing ever reads it at alarm time: the alarm is purely a notification-channel sound (channel built in thermostat_monitor.dart:742-793) played at whatever the user's alarm-stream volume happens to be. Grep shows volumeBoost is only writt
  R: Either implement it — when the full-screen alarm page opens (and/or from the service isolate), use audio_session + just_audio to play the chosen tone on the alarm usage and, when volumeBoost is on, temporarily raise STREAM_ALARM volume to max, restoring on acknowledge — or remove

* [high|feature-idea|confirmed] No 'will my alarm actually fire?' end-to-end health check @app/lib/features/settings/view/settings_page.dart:592
  D: The alarm chain has ~6 independently breakable links: notification permission (requested once at startup, notification_permission.dart:16-32, then never surfaced again if later revoked), full-screen-intent access (never checked), the alarm notification channel possibly muted/re-sounded by the user in system settings, battery exemption (visible in S
  R: Add a 'Monitoring health' panel at the top of Settings (or the Thermostats page) with green/red rows: notifications allowed, full-screen alarms allowed, alarm channel audible, battery exemption granted, monitoring service running (FlutterForegroundTask.isRunningService), last suc

* [medium|bug|confirmed] Hysteresis exists in the model and alarm logic but no UI can ever enable it @app/lib/features/thermostats/models/thermostat.dart:94
  D: The flapping-prevention logic in isThermostatReadingOutOfRange (thermostat_reading_utils.dart:42-67, with the 1.0degC re-entry buffer per Spec.md 3.2) is fully implemented and tested, but it is dead in production: ThermostatEntries.hysteresisEnabled defaults to false (thermostat_database.dart:25-26), ThermostatDraft has no hysteresis field (thermos
  R: Add hysteresisEnabled to ThermostatDraft and a 'Prevent alarm flapping (1degC buffer)' switch in the thermostat form dialog (default on for new thermostats, per Spec 14); plumb through create/update. Effort: small (~half a day plus tests).

* [medium|design-improvement|confirmed] Per-thermostat event log (Spec's diagnostics) is missing — no way to answer 'why didn't it alarm last night?' @app/lib/features/thermostats/data/thermostat_database.dart:39
  D: Spec.md 3.7 requires a diagnostics view of the last 50 events per thermostat and the data model (Spec.md section 5) includes an EventLog table capped at 5,000 rows; neither exists. The DB keeps exactly one statusMessage per thermostat (ThermostatStateEntries, thermostat_database.dart:39-65), overwritten every poll, and the 'developer log export' (d
  R: Add an EventLog Drift table (thermostatId, time, level, message), append from the state-transition points that already exist (saveState status changes, recordOutOfRangeAndShouldAlarm fire decision, snooze/silence writes), cap at ~5,000 rows in pruneRetention, show the last ~20 on

* [low|feature-idea|downgraded] Configuration export/import (QR or file) for a cheap second-phone redundancy story @-
  D: The architecture makes multi-device redundancy nearly free — the gists are public, all config is local, and any phone running FarmCtl with the same gist IDs monitors independently (no server, no account). That is the most realistic mitigation for the single-point-of-failure phone (dead battery, left in the truck, Do Not Disturb). But today setting 
  R: Add 'Export setup' (share a small JSON file, or render a QR code) and 'Import setup' (file pick / QR scan) in Settings, reusing the serialization in developer_log_exporter.dart and ThermostatRepository.create. Explicitly market it in-app as 'add a backup phone'. Effort: small-med
  V: Facts check out (no import/share path exists in app/lib; developer_log_exporter.dart:71-83 already serializes every needed field; gists are public so a second phone works independently), and the idea genuinely fits this architecture. But it is nowher

* [low|feature-idea|downgraded] Unacknowledged-alarm escalation: re-alerts exist but never get louder or more insistent @app/lib/features/thermostats/data/thermostat_reading_utils.dart:5
  D: While out of range, the monitor re-fires the alarm every max(pollInterval, 5 min) thanks to the kAlarmRateLimit gate (thermostat_reading_utils.dart:5-40) — good. But every re-fire is identical: same channel, same sound, same (possibly quiet) volume, and each new show() replaces the previous notification with the same ID (thermostat_monitor.dart:831
  R: Add escalation tiers driven by (now - lastAlarmAt) while status stays outOfRange and no snooze/silence was recorded: e.g. after 2 missed re-alerts, switch to a longer/looping tone and max the alarm stream (ties into fixing volumeBoost), extend vibration pattern, and prefix the ti
  V: Mechanics verified: shouldTriggerAlarm (thermostat_reading_utils.dart:13-40) re-fires after the 5-min rate limit with no unacknowledged-duration concept, and NotificationAlarmDispatcher.showAlarm (thermostat_monitor.dart:813-840) posts an identical n

* [low|ux-improvement|downgraded] History insight: no min/max/avg summary for the selected range (data is computed, then hidden in a11y text) @app/lib/features/thermostats/widgets/thermostat_history_chart.dart:92
  D: The chart already computes dataMin/dataMax for the visible range but only exposes them in the screen-reader semantics string (thermostat_history_chart.dart:62-97); a sighted farmer has to eyeball the trace to answer the questions they actually ask: 'how cold did the barn get last night?' and 'how close to the limit did we run this week?'. There is 
  R: Add a compact stats row above the chart (Min x.x deg at HH:mm - Max x.x deg at HH:mm - Avg x.x deg), with min/max dots on the trace; consider a follow-up opt-in daily digest notification per thermostat ('Overnight: 2.1-9.4degC, no excursions') posted by the existing monitor run o
  V: Verified: thermostat_history_chart.dart:62-63 computes dataMin/dataMax and lines 92-97 expose them only in the Semantics value string; the detail page (thermostat_detail_page.dart:100-198) renders card + range selector + chart with no numeric summary

* [medium|feature-idea|confirmed] CSV export of readings (spec'd, unimplemented) for records, vets and insurance @app/lib/features/settings/services/developer_log_exporter.dart:32
  D: Spec.md 7.2D ('Export data (CSV per thermostat)') and section 9 ('user can export CSV per thermostat and selected range') are unimplemented — the only export is the developer JSON snapshot which contains zero temperature samples. For this user, temperature history is not just debugging data: livestock/produce incidents lead to insurance claims and 
  R: Add 'Export readings (CSV)' on the thermostat detail page (current range) and/or in Settings (per thermostat, all data): stream watchHistory/DB rows to a timestamp,valueC,source CSV in app documents and hand it to the Android share sheet. Effort: small (~1-2 days), reuses Thermos

* [low|feature-idea|downgraded] Home-screen widget for glanceable barn temperatures @-
  D: A farmer's real usage pattern is dozens of anxious glances per day, each currently costing unlock -> open app -> wait for list. A 2x2/4x2 home-screen widget showing each thermostat's name, last value, status color and 'updated Xm ago' converts that to zero-tap. The plumbing is unusually cheap here because the background monitor already wakes every 
  R: Use the home_widget package: after each _runMonitorTask (thermostat_monitor.dart:388-401) push name/value/status/lastFetchedAt per thermostat and trigger a widget update; render a simple RemoteViews list. Start Android-only, no interactivity beyond launching the app. Effort: medi
  V: Facts are right (no home_widget dependency in pubspec.yaml; the monitor run does end each cycle with fresh state rows it could push), but this is a pure nice-to-have not in docs/Spec.md, the 'dozens of anxious glances' usage pattern is asserted witho

* [low|ux-improvement|confirmed] Card 'stale' threshold is a fixed 30 minutes, disconnected from the poll interval @app/lib/features/thermostats/widgets/thermostat_card.dart:54
  D: The list/detail card flags a reading as stale after a hardcoded 30 minutes (thermostat_card.dart:54-56), while the poll interval is user-configurable from 1 to 30 minutes (settings_page.dart:81). At a 30-minute interval every reading flirts with being marked stale (false alarm -> banner blindness); at a 1-minute interval the farmer isn't warned unt
  R: Compute staleness as max(3 x pollInterval, ~10 min) using the alertConfigProvider already available via Riverpod, and show the same computed threshold in the detail view. Effort: trivial (~2 hours incl. widget test update).

* [low|feature-idea|confirmed] Per-thermostat threshold schedules (day/night ranges) @app/lib/features/thermostats/models/thermostat.dart:22
  D: Each thermostat has exactly one static minC/maxC pair (thermostat.dart:22-23). Real farm targets are time-dependent — greenhouses tolerate cooler nights, brooders need different day/night bands — so users either set the range to the loosest union (missing real excursions) or get nightly false alarms (training them to snooze). This is a genuine gap 
  R: Ship as an optional 'night range' (start time, end time, alternate min/max, nullable columns on ThermostatEntries); pick the active band inside isThermostatReadingOutOfRange via an injected clock so hysteresis and tests keep working. Do it after the trust-critical items above. Ef

* [low|ux-improvement|confirmed] Chart interactivity below spec: no pan/zoom and no cross-thermostat comparison @app/lib/features/thermostats/widgets/thermostat_history_chart.dart:186
  D: Spec.md 7.2C calls for 'Interactive pan/zoom; data point tooltip'; the current chart implements tooltips only (LineTouchData at thermostat_history_chart.dart:186-204) — the fullscreen page adds screen real estate but not navigation, so inspecting 'that dip three days ago' inside a 30-day range means squinting. There is also no way to overlay two th
  R: Phase 1: horizontal drag-to-pan + pinch zoom on the fullscreen chart (fl_chart supports clamped minX/maxX windows driven by a GestureDetector; keep the range selector as zoom presets). Phase 2: multi-select thermostats on the fullscreen chart rendering one LineChartBarData per se

+ MISSED [medium] Per-thermostat 'Monitor on/off' toggle is spec-required, fully plumbed, and has no UI — exactly parallel to the hysteresis finding @app/lib/features/thermostats/widgets/thermostat_form_dialog.dart: docs/Spec.md:110 (3.6 'Per-thermostat: enable/disable monitoring'), Spec.md:225 (7.2A card 'Toggle: Monitor on/off') and Spec.md:230 (7.2B form 'Monitoring (switch)') all require a per-thermostat monitoring switch. The monitoringEnabled column exists (thermostat_database.dart:28-29, default true), the model carries it (thermostat.dart:25), and the 

+ MISSED [high] MainActivity lacks showWhenLocked/turnScreenOn, so the spec'd dismiss-keyguard alarm surface cannot appear even after adding USE_FULL_SCREEN_INTENT @app/android/app/src/main/AndroidManifest.xml: The reviewer's top finding (missing USE_FULL_SCREEN_INTENT) and its recommendation are incomplete: grep over app/android returns no showWhenLocked, turnScreenOn, or setShowWhenLocked anywhere. When a full-screen intent fires on a locked device, Android launches the target activity, but it renders BEHIND the keyguard unless the activity declares and

+ MISSED [low] Detail page is missing the spec'd quick Snooze/Silence controls @app/lib/features/thermostats/view/thermostat_detail_page.dart: docs/Spec.md:237 (7.2C) requires the details screen's current panel to offer 'quick controls: Snooze, Silence'. The detail page (thermostat_detail_page.dart:100-198) renders only ThermostatCard plus the history card; grep shows snooze/silence actions exist solely on the alarm fullscreen page (alarm_fullscreen_page.dart:295-317) and the notification

+ MISSED [low] Thermostats list search (spec'd) not implemented @app/lib/features/thermostats/view/thermostats_page.dart: docs/Spec.md:219 (7.2A) specifies 'Header: Thermostats + search' on the list screen; no search field or filter exists in thermostats_page.dart (grep for search in the view layer returns nothing). For a farm with a handful of sensors this is genuinely minor — worth recording only as a deliberate spec deviation (and ideally the spec should be amended
SKEPTIC: A genuinely thorough features review: all 14 findings survived source verification — every file:line citation I checked was accurate (including subtle claims like fetchedAt being device receive time and volumeBoost having zero consumers), the two flagship gaps (missing USE_FULL_SCREEN_INTENT and no 
============================================================
META: {
 "crossCuttingObservations": [
  "The alarm trust chain is the convergence point of the whole review: four of six dimensions independently placed their top findings on alarm delivery (dead-sensor staleness, missing USE_FULL_SCREEN_INTENT, silent notification-permission denial, no-op audibility settings, 22%-covered monitor orchestration). The portfolio is well-aimed: the app's single job is 'scream when the barn is cold', and that is exactly where the confirmed highs cluster.",
  "Three root causes generate a large fraction of all findings and should be fixed as root causes, not symptom-by-symptom: (a) no concept of data age vs fetch age (produces dead-sensor blindness, unreachable stale badge, misleading 'Last update', fixed 30-min threshold, frozen 'since' cutoff); (b) two competing token-resolution mechanisms in the history pipeline (produces the client leaks, the budget-vs-transport disagreement, and the silent dead-token masking); (c) dead plumbing shipped ahead of its UI (volumeBoost, hysteresisEnabled, monitoringEnabled, fetchHistory/replaceHistory, just_audio/audio_session deps).",
  "Skeptic calibration was consistent and credible across all six dimensions: roughly 16 downgrades and zero rejections out of ~70 verdicts, every downgrade backed by a verified mechanism (e.g. the 20-vs-400 budget correction, Drift streams not crossing isolates, the loadConfig token scrub). I spot-checked five verdicts across three skeptics (including all-confirmed UX) and all five held against the code. The reviewers' systematic bias was severity inflation, not fabrication — the skeptic layer earned its keep.",
  "The reviewers made good use of .agent/ history — only one finding (notification-id collision) re-reported a previously known and accepted risk, and several verdicts correctly cited prior triage decisions (L-3, L-7, bug-hunt #1/#2, UI-07) instead of re-litigating them.",
  "The working tree currently has an uncommitted app/pubspec.lock modification (transitive downgrades: meta 1.18.0->1.17.0, test 1.31.0->1.30.0, shared_preferences_android, synchronized), which AGENTS.md's 'keep pubspec.lock committed, do not float' guardrail flags — and which is a symptom of the unpinned Flutter toolchain blind spot below. It should be reverted or deliberately committed.",
  "The 'single-user farm app' framing was used consistently and appropriately by all skeptics to cap severities — but notably it was never allowed to excuse the safety-critical findings (dead sensor, full-screen intent, permission denial), which is the right calibration for this product."
 ],
 "duplicates": [
  "Missing USE_FULL_SCREEN_INTENT permission: security 'fullScreenIntent is requested but USE_FULL_SCREEN_INTENT is not declared in the manifest' (medium) = features 'Full-screen alarm cannot appear: USE_FULL_SCREEN_INTENT permission is never declared' (critical->high). Same manifest gap; features' missed-issue (showWhenLocked/turnScreenOn) completes both.",
  "Dead/silent sensor undetectable: correctness 'Silent sensor death is undetectable: gist data staleness is never checked' (high) = features 'A dead sensor never alarms: no stale-data detection' (critical). Identical root issue reported as bug vs feature-idea; ux's missed-issue 'Last update advances on failed fetches / stale badge unreachable' and features' 'Card stale threshold is a fixed 30 minutes' are the same no-data-age root cause.",
  "Boost volume no-op: ux 'Boost volume setting is a complete no-op' (high) = features ''Boost volume' setting is a dead toggle' (high); ux's missed-issue 'Dead audio dependencies (just_audio, audio_session)' is the same abandoned feature.",
  "Silent notification-permission denial: security 'Denied/revoked notification permission silently disables every alarm' (high->medium) = ux 'Denied notification permission silently kills all alarms with no in-app warning' (high); features 'No will-my-alarm-actually-fire end-to-end health check' (high) subsumes it as the broader gap.",
  "History backfill rate-limit exhaustion: correctness 'refreshHistory pages through the gist's entire commit history — no page cap' (high) = security 'History backfill can burn the GitHub rate limit: up to 400 revision fetches per thermostat per poll' (medium). Same pipeline, correctness has the stronger mechanism (deterministic stride rejection makes full-history walks the steady state).",
  "Unclosed ThermostatHttpClient leak: architecture '_resolveNetworkWithToken constructs a new client per call and never closes' + 'thermostatNetworkProvider never closes the replaced client' = correctness 'ThermostatHttpClient instances are created per page/per revision and never closed' = security 'ThermostatHttpClient instances are created per revision fetch / per provider rebuild and never closed'. Triple-reported; architecture's missed-issue (buildMonitorDependencies leak per background run) is the fourth instance of the same bug.",
  "Frozen 'since' cutoff on history charts: architecture missed-issue 'thermostatHistoryProvider freezes its since cutoff at first build' = correctness finding 'thermostatHistoryProvider freezes its since cutoff for the app session' (medium).",
  "Budget-vs-transport token disagreement in refreshHistory: architecture missed-issue 'request budget decided by the injected client but requests run on a different token-bearing client' = the correction embedded in three skeptics' leak-downgrade verdicts (correctness, security, architecture) = part of architecture's 'refreshHistory god method' downcast finding.",
  "Dead plumbing with no UI: ux 'Per-thermostat monitoring and hysteresis toggles exist in the data model but have no UI' (medium) = features 'Hysteresis exists in the model and alarm logic but no UI can ever enable it' (medium) + features missed-issue 'Per-thermostat Monitor on/off toggle is spec-required, fully plumbed, and has no UI'.",
  "Raw exception text bypassing humanizeError: security 'Raw settings/alarm error surfaces bypass humanizeError' + security missed-issue 'Raw exception text is persisted into DB statusMessage' = ux 'Settings failure screen dumps a raw exception' + ux missed-issue 'Raw exception text can reach the card via the catch-all statusMessage'.",
  "Chart interactivity below spec (no pan/zoom): ux 'History chart has tooltips only — no pan/zoom, no min/avg/max in aggregated tooltips' = features 'Chart interactivity below spec: no pan/zoom and no cross-thermostat comparison'; features' 'History insight: no min/max/avg summary' overlaps the same chart-insight gap.",
  "No search on the thermostats list: ux 'No search on the thermostats list (spec 7.2A)' = features missed-issue 'Thermostats list search (spec'd) not implemented'.",
  "No diagnostics/event log: ux 'No diagnostics/event-log surface (spec 3.7/7.2C)' (low) = features 'Per-thermostat event log is missing — no way to answer why didn't it alarm last night' (medium).",
  "Dead history API: architecture 'Production-dead history API: fetchHistory, replaceHistory' = correctness missed-issue 'Dead code: ThermostatHttpClient.fetchHistory and ThermostatRepository.replaceHistory have no production callers'."
 ],
 "blindSpots": [
  {
   "area": "CI/release toolchain pinning (.github/workflows/ci.yaml, release.yaml)",
   "description": "Both workflows use subosito/flutter-action@v2 with channel: stable and no flutter-version key, so the exact Flutter/Dart toolchain that builds the SIGNED RELEASE APK floats with every upstream stable release — a Flutter upgrade can silently change the shipped alarm binary (or break format/analyze gates) with zero repo change, and the tag-matches-pubspec check would not notice. The security reviewer audited action tag-pinning and secrets handling but never the toolchain version. Verified: no flutter-version anywhere in either workflow; corroborated by the live uncommitted app/pubspec.lock drift in this workspace (meta 1.18.0->1.17.0, test 1.31.0->1.30.0 downgrades) showing different environments already resolve differently.",
   "severity": "medium"
  },
  {
   "area": "Documentation accuracy (README.md, app/README.md, docs/)",
   "description": "No reviewer examined the docs for accuracy. Verified defects: root README.md links [Spec.md](Spec.md) but no Spec.md exists at the repo root — the spec lives at docs/Spec.md (this stale pointer even propagated into the review orchestration prompt itself, which claimed 'Spec.md at repo root'); README describes test/ as 'Placeholder widget test scaffolding for future coverage' when the suite is substantial with a ratcheted 70% CI gate; README describes pubspec.yaml deps as 'for future iterations' when Drift/Dio are fully in production; app/README.md is the untouched 'A new Flutter project' boilerplate. For a repo whose AGENTS.md explicitly mandates keeping README accurate, this is a genuine unreviewed surface.",
   "severity": "low"
  },
  {
   "area": "Non-Android platform folders (app/ios, app/windows, app/linux, app/macos, app/web)",
   "description": "Five platform scaffolds ship in the repo but no reviewer assessed whether the app degrades sanely on them or whether they should exist at all. Verified: _supportsForegroundService (thermostat_monitor.dart:59) correctly guards the foreground service (:147) and notification launch paths, but initializeBackgroundMonitoring's Workmanager().initialize/registerPeriodicTask calls (~:280-287) have NO platform guard — on desktop/iOS they throw (MissingPluginException/unsupported), which main.dart:10-15's try/catch swallows, leaving a silently monitor-less, view-only app with only a debugPrint. Low because README declares Android as the target, but the unguarded call and the decision to keep the scaffolds were never examined.",
   "severity": "low"
  },
  {
   "area": "Branding and launcher identity (app/android/app/src/main/AndroidManifest.xml, mipmap resources)",
   "description": "The security reviewer caught applicationId com.example.farmctl but nobody covered the adjacent user-facing branding: android:label=\"farmctl\" (lowercase, AndroidManifest.xml:10) and the stock Flutter ic_launcher icon in all mipmap densities. For an app whose alarm notification and launcher entry the farmer must recognize at 3am, a default Flutter icon labeled 'farmctl' is a small real gap, and it should be fixed in the same (costly, identity-changing) pass as the applicationId rename since both get harder after wider install.",
   "severity": "low"
  },
  {
   "area": "Localization/i18n posture (app/lib/app.dart)",
   "description": "No reviewer mentioned i18n at all. Verified: MaterialApp declares supportedLocales: [Locale('en')] with the global delegates wired (app.dart:23-28), and every user-facing string in lib/ is hardcoded English — there is no arb/l10n pipeline despite flutter_localizations being a dependency and README claiming 'localization scaffolding (English)'. For a single-user app this is an acceptable non-goal, but it is an undocumented one: the scaffolding's existence without any extraction pipeline is exactly the kind of dead plumbing the reviews flagged elsewhere.",
   "severity": "low"
  },
  {
   "area": "Developer tooling: .githooks/ and .pre-commit-config.yaml",
   "description": "The repo ships two parallel hook systems (.githooks/pre-commit and a .pre-commit-config.yaml running dart format + flutter analyze) that no reviewer looked at. The pre-commit analyze hook runs flutter analyze without first running build_runner, so on a fresh clone (generated code is gitignored per .gitignore:35-36) the hook fails on missing *.g.dart types — contradicting the CLAUDE.md instruction that build_runner must always precede analyze. Minor, but it is the contributor's first-touch experience and was fully unexamined.",
   "severity": "low"
  }
 ],
 "topPriorities": [
  "1. Detect dead sensors: compare the gist's updated_at / newest revision timestamp to now and alarm (or at minimum badge) when data age exceeds a threshold — the confirmed correctness-high/features-critical duplicate. A barn power failure currently kills heater and sensor together while the app shows fresh green OK forever; this is the single largest gap between what the app promises and what it does.",
  "2. Fix the full-screen alarm chain as one unit: add USE_FULL_SCREEN_INTENT to AndroidManifest.xml AND showWhenLocked/turnScreenOn on MainActivity (the features missed-issue), then verify on a locked physical device. Without both, the spec's dismiss-keyguard 3am alarm surface silently never appears on API 29+.",
  "3. Surface alarm-delivery health in-app: a persistent banner/settings tile when notification permission is denied or revoked, plus exposing lastMonitorRunAt (already persisted, never shown). Merges the security/ux permission findings with features' 'will my alarm fire tonight?' gap — the alarm chain has multiple silently-revocable links and zero visibility today.",
  "4. Make the audibility settings honest: implement or remove the no-op 'Boost volume' toggle (and the dead just_audio/audio_session deps), fix the vibrate toggle by deriving the channel id from the vibrate setting as well as soundUri (channel settings are immutable once created on API 26+), and consider FLAG_INSISTENT for alarm-clock-like persistence per Spec 3.5's stated goal.",
  "5. Bound and cheapen the history backfill: add a page cap to refreshHistory's commit-paging loop and send If-None-Match on gist polls (ETag is already captured and persisted, 304s are rate-limit-free). Together these protect the shared GitHub rate limit that the alarm-path fetch depends on — the confirmed correctness-high shows the loop walks the entire commit history every run once stride-rejection saturates. Add the multi-page fully-cached regression test the testing review specified.",
  "6. Make snooze/silence visible and reversible outside the alarm screen: show the state on ThermostatCard and the detail page, add the spec'd quick Snooze/Silence controls there, and wire the existing-but-never-called updateSilenceUntilOk(id,false). A half-asleep notification tap can currently silence a thermostat for the entire duration of a dangerous excursion with no morning-after trace.",
  "7. Put the background delivery path under test: extract injectable seams for _runMonitorTaskLocked, _handleNotificationResponse (hardcoded ThermostatDatabase()), and the watchdog; actually tap the alarm page's snooze/silence/acknowledge buttons in widget tests. thermostat_monitor.dart at 22% coverage is exactly where the last three releases' bugs lived, and main.dart/notification_permission.dart are invisible to the 70% gate.",
  "8. Track lastSuccessAt separately from lastAttemptAt and drive the hero 'Last update' text and the stale badge from data/success age, not fetch-attempt time; also re-derive the history chart's frozen 'since' cutoff (or make the family providers autoDispose). This closes the whole no-data-age cluster: misleading timestamps, unreachable stale badge, ever-growing chart windows.",
  "9. Expose the dead-plumbed per-thermostat controls: Monitoring on/off and Hysteresis switches in ThermostatFormDialog/ThermostatDraft (both spec-required, both fully honored by the alarm logic, both permanently stuck at defaults). Cheap, and hysteresis directly reduces the re-alarm nuisance that trains snooze habits.",
  "10. Consolidate the fetch-result-application logic (triplicated across monitor runner, ThermostatService.refresh, and _saveTestedState — the class of duplication that already shipped the PR #27 drift bug) and unify token resolution into one place, which simultaneously fixes the client-leak family, the budget-vs-transport disagreement, and the raw-'Unexpected error: $error' statusMessage hygiene; move the monitor's history try/catch inside the per-thermostat loop so one bad gist can't starve every other chart. Alongside: pin the Flutter version in ci.yaml/release.yaml and reconcile the currently-dirty pubspec.lock (meta blind spot)."
 ],
 "overallVerdict": "Codebase health: genuinely good for a solo hobby project with safety ambitions — transactional alarm arbitration, fail-closed parsing, WAL-aware dual-isolate DB use, secure token migration with read-back-before-scrub, a real coverage gate, and evidence that three prior internal review cycles actually landed their fixes. But the product has one honest headline problem the review correctly converged on from two directions: the alarm trust chain has multiple silent total-failure modes (dead sensor shows green forever, full-screen alarm can never appear, denied permission kills everything invisibly, two audibility settings are no-ops), and the exact code that delivers alarms is the least-tested file in the repo at 22%. Everything else — architecture debt, leaks, spec gaps — is second-order next to closing those. Review quality: high. All six reviewers were code-grounded with accurate citations, cross-checked .agent history (only one known-issue re-report), and the two flagship discoveries (dead-sensor blindness, unbounded pagination) are novel and real. The skeptic layer worked as designed: ~16 downgrades, zero rejections, every downgrade I audited backed by a correct mechanism, and my five spot-checks across three skeptics (including the suspicious all-confirmed UX skeptic, which turned out to reflect an unusually accurate underlying review) all held against the code. The process's weaknesses: systematic pre-skeptic severity inflation, heavy cross-dimension duplication (14 duplicate clusters — the same six issues consumed a large share of six reviewers' attention), and a shared blind spot for everything outside app/lib and the Android manifest: unpinned CI/release toolchain, stale README (including a broken Spec.md link that even the review prompt inherited), unguarded non-Android init, default branding, and dev tooling. None of the blind spots outranks the confirmed findings, which is itself a good sign for the review's coverage of what matters."
}